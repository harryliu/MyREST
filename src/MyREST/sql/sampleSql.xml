<?xml version="1.0" encoding="utf-8" ?>
<root version="1.0">
	<sql id="queryAllActors">
		<![CDATA[
		  select actor_id , first_name FirstName, last_name LastName, last_update LastUpdate
           from actor
		]]>
		<parameters>
			<parameter name="@id" dataType="Int32"  direction="Input" format="" ></parameter>
		</parameters>
	</sql>

	<sql id="queryAllActors">
		<![CDATA[
		  select actor_id , first_name FirstName, last_name LastName, last_update LastUpdate
           from actor where actor_id=@id
		]]>
		<parameters>
			<parameter name="@id" dataType="Int32"  direction="Input" format="" ></parameter>
		</parameters>
	</sql>

	<sql id="">
		<![CDATA[
		insert into blog (`id`, `name`, `title`, `content`)
		values (#{id}, #{name}, #{title}, #{content})
		]]>
	</sql>

	<sql id="">
		<![CDATA[
		update blog
		set `name` = #{name}, `title` = #{title}, `content` = #{title}
		where id = #{id}
		]]>
	</sql>

	<sql id="">
		<![CDATA[
		delete from blog where id = #{id}
		]]>
	</sql>
</root>